import { SiteHeader } from "@/components/chrome/SiteHeader";

/** Same SiteHeader as the rest of the consumer site. */
export function CategoriesChrome() {
  return <SiteHeader askAiAccent="electric" />;
}
